import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-reregister',
  standalone: true,
  imports: [RouterLink],
  templateUrl: './reregister.html',
  styleUrls: ['./reregister.css'],
})
export class Reregister {}
